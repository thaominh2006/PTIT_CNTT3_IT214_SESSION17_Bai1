server:
  port: 8080

spring:
  application:
    name: session17-bai1

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 10

logging:
  level:
    org.springframework.cache: DEBUG
    io.lettuce.core: DEBUG
